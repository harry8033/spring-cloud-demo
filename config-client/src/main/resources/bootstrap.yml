spring:
  application:
    name: config-client
  cloud:
    config:
      profile: dev
      uri:  http://localhost:8080/config-server
server:
  port: 8081

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:9000/eureka/
